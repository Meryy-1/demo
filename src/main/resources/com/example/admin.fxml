<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<StackPane xmlns="http://javafx.com/javafx/8.0.171" xmlns:fx="http://javafx.com/fxml/1" 
           fx:controller="com.example.AdminController" stylesheets="@styles.css" styleClass="root">
    
    <ScrollPane fitToWidth="true" fitToHeight="true" style="-fx-background: transparent; -fx-background-color: transparent;">
        <StackPane minHeight="480" minWidth="600">
            <VBox alignment="TOP_CENTER" spacing="25" styleClass="main-container" maxWidth="600" style="-fx-padding: 10px;">
        <!-- Header -->
        <HBox alignment="CENTER_LEFT" spacing="15">
            <Button text="← Logout" onAction="#handleLogout" styleClass="submit-btn" 
                    style="-fx-background-color: #6c757d;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <VBox alignment="CENTER_RIGHT">
                <Label text="Admin Panel" styleClass="restaurant-title" 
                       style="-fx-font-size: 28px;"/>
                <Label fx:id="adminCodeLabel" text="Admin: N/A" styleClass="field-label" 
                       style="-fx-text-fill: #6c757d;"/>
            </VBox>
        </HBox>
        
        <Separator styleClass="title-separator"/>
        
        <!-- Admin Dashboard -->
        <VBox alignment="CENTER" spacing="20" styleClass="selection-container">
            <Label text="Administration" styleClass="selection-label"/>
            
            <VBox spacing="15" alignment="CENTER" style="-fx-padding: 20px;">
                <!-- Manage Clients -->
                <Button text="👥 Manage Clients" styleClass="option-btn" 
                        onAction="#handleManageClients" prefWidth="300"/>
                
                <!-- Manage Orders -->
                <Button text="📦 Manage Orders" styleClass="option-btn" 
                        onAction="#handleManageOrders" prefWidth="300"/>
                
                <!-- Manage Menu -->
                <Button text="🍽️ Manage Menu" styleClass="option-btn" 
                        onAction="#handleManageMenu" prefWidth="300"/>
                
                <!-- View Reports -->
                <Button text="📊 View Reports" styleClass="option-btn" 
                        onAction="#handleViewReports" prefWidth="300"/>
                
                <!-- Settings -->
                <Button text="⚙️ Settings" styleClass="option-btn" 
                        onAction="#handleSettings" prefWidth="300"/>
            </VBox>
        </VBox>
        
        <!-- Status Label -->
        <Label fx:id="statusLabel" text="" styleClass="status-label"/>
        
            </VBox>
        </StackPane>
    </ScrollPane>
    
</StackPane>
